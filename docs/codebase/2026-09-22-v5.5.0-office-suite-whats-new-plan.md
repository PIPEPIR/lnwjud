# lnwjud v5.5.0 — Complete Office Suite Tools + What's New UX Plan

Date: 2026-09-22
Target: lnwjud 5.5.0
Branch when implementation starts: dev
Status: IMPLEMENTATION IN PROGRESS on dev for v5.5.0
Implementation baseline was re-checked before mutation. v5.5.0 now also includes the additive scheduler-resume and MCP connection-transport scope recorded below; those additions must preserve all existing v5.4.x ngrok/OAuth/Secure MCP Tunnel behavior by default.

## Destination

v5.5.0 should turn the existing Office capability into a production-grade Office automation platform that ChatGPT can use directly through lnwjud, while preserving lnwjud permission, Active Project, recovery, audit, capability-readiness, and platform boundaries.

The release has two primary outcomes:

1. Complete, practical Office tooling that can read, create, edit, inspect, validate, convert, compare, and automate supported Microsoft Office / Microsoft 365 workloads with truthful provider capability reporting.
2. A user-facing “What’s New” experience beside the app version that opens a localized release-notes modal for the currently installed version.

“Complete Office tooling” does not mean pretending every platform exposes identical native APIs. It means every advertised action must either work through a verified provider or return an explicit dependency/unsupported result. No feature may silently fall back to UI clicking and report native success.

## Current repository baseline

The existing implementation already provides the foundation and must be evolved rather than bypassed.

Current Office-related surfaces include:

- Generic office capability with excel, word, powerpoint, and outlook apps.
- Generic actions currently include status, read, read_text, sheets, list_folders, list_messages, write, replace, merge, and save_as.
- inspect_workbook.
- render_excel_preview.
- compare_workbook_layout.
- office_ppt, currently limited mainly to read and save_as.
- office_outlook, currently limited to folder/message listing.
- docx_merge with dry-run / confirmation / file-recovery integration.
- Windows-native Office provider is available on the current Windows host.
- macOS/Linux Office backends currently preserve the provider boundary but are dependency-gated and do not implement native parity.
- Tool catalog, mutation policy, upgrade catalog, schema registry, audit, Active Project, Full Bypass, Recovery Trash, and external-file mutation protection already exist and should remain authoritative.

The desktop title bar currently renders the app version inside AppShell as the update/check/install button. The sidebar also displays the version as static text. v5.5.0 must preserve the current update button behavior and add a separate “?” affordance for release notes rather than overloading the existing version action.

## Product principles

### Truthful capability reporting

Every Office tool must expose provider, readiness, dependency state, supported actions, limitations, and platform. A tool must not claim ready when the installed provider cannot perform the requested action.

### Native structured APIs before desktop UI automation

Preferred order:

1. Native Office object model / local structured provider.
2. Microsoft 365 / Microsoft Graph where the requested workload is actually supported.
3. Open XML / structured file provider where fidelity is sufficient and explicitly reported.
4. LibreOffice UNO where relevant on Linux and where compatibility is validated.
5. Desktop UI automation only as an explicit, separately named fallback when no structured provider exists. It must never masquerade as COM, Graph, or Open XML.

### Backward compatibility

Keep the existing generic office capability operational for existing clients. New high-level tools should route through a shared Office runtime so behavior is not duplicated.

### Bounded model context

Do not return entire large workbooks, documents, mailboxes, decks, notebooks, or sites by default. Every read API must provide paging/ranges/limits and bounded previews.

### Recoverable mutations

File mutations must use the same external-file safety and replacement-backup model already used by docx_merge. A failed operation must not leave a partially replaced source file when an atomic/temp-copy workflow is possible.

### Explicit risk

Reading is not equivalent to sending mail, deleting a meeting, refreshing external data, running VBA, publishing a presentation, or overwriting a document. Mutation and dangerous actions must remain separately classified.

## Target architecture

Create an Office Suite v2 layer rather than adding unrelated one-off tools.

Logical flow:

ChatGPT / MCP client
→ lnwjud tool contracts
→ OfficeSuiteRuntime
→ provider router
→ one verified provider
→ output normalization / bounded result
→ audit + recovery + telemetry

Recommended provider layers:

- Windows Office COM provider: authoritative high-fidelity local provider for installed Microsoft Office apps on Windows.
- macOS Office automation provider: explicit Apple Events / supported native automation only for operations confirmed to work. No fake parity.
- LibreOffice UNO provider: Linux/local document fallback for supported document/spreadsheet/presentation operations.
- Open XML provider: cross-platform structural DOCX/XLSX/PPTX read/write/inspect/compare for operations that can preserve required fidelity.
- Microsoft Graph provider: Microsoft 365 cloud workloads such as Outlook mail/calendar/contacts, OneDrive, SharePoint, OneNote, Teams, and Excel workbook APIs where Graph supports the requested operation.
- UI fallback provider: opt-in only, separately reported, never selected silently.

Provider selection must be deterministic and observable. The result should say which provider handled the request.

## Proposed Office tool surface

Avoid creating hundreds of tiny MCP tools. Prefer a bounded set of first-class tools with typed action enums and app-specific schemas. This keeps tool discovery practical while still providing full functional breadth.

### office_status

Purpose:

- Discover installed/local/cloud Office providers.
- Report app availability.
- Report provider versions where obtainable.
- Report Microsoft 365 sign-in state without exposing tokens.
- Report supported action matrix by app/provider/platform.
- Report degraded/optional dependencies.
- Report whether macros, external data refresh, print, send, and destructive actions are policy-enabled.

Must be read-only.

### office_word

The Word tool should cover the complete practical document lifecycle.

Read / inspect actions:

- create_document metadata preview before mutation.
- open / inspect_document.
- read_text.
- read_range.
- get_structure.
- get_sections.
- get_paragraphs.
- get_runs.
- get_styles.
- get_headings.
- get_tables.
- get_images metadata.
- get_headers_footers.
- get_bookmarks.
- get_hyperlinks.
- get_fields.
- get_comments.
- get_revisions / tracked changes.
- get_footnotes.
- get_endnotes.
- search.
- document_properties.
- page_setup.
- protection_status.

Write / edit actions:

- create.
- insert_text.
- append_text.
- prepend_text.
- replace_text.
- replace_range.
- delete_range.
- set_font.
- set_character_format.
- set_paragraph_format.
- set_alignment.
- set_spacing.
- apply_style.
- create_or_update_style.
- add_heading.
- add_page_break.
- add_section_break.
- insert_table.
- update_table_cells.
- add/delete rows and columns.
- merge/split cells.
- set_table_style.
- insert_image.
- replace_image.
- add_textbox/shape when supported by provider.
- add/edit hyperlink.
- add/remove bookmark.
- add/update header/footer.
- page numbering.
- margins/orientation/paper size.
- add footnote/endnote.
- add/update table of contents.
- add/update field.
- add/read/resolve/delete comment.
- enable track changes.
- accept/reject selected revisions.
- mail merge preview and execution.
- merge documents.
- save.
- save_as.
- export_pdf.
- export_text.
- print with explicit confirmation.
- protect/unprotect when provider supports it and password handling is ephemeral.

Validation / visual actions:

- inspect_docx.
- render_docx_preview.
- compare_docx_structure.
- compare_docx_layout.
- detect text overflow / clipped objects where render provider supports it.
- verify page count, headings, tables, headers/footers, and key text after mutation.

### office_excel

The Excel tool should support structured workbook automation instead of only generic range reads/writes.

Read / inspect actions:

- create_workbook metadata preview.
- list_sheets.
- inspect_workbook.
- used_range.
- read_range.
- read_values.
- read_formulas.
- read_number_formats.
- read_styles.
- read_tables.
- read_named_ranges.
- read_charts.
- read_pivots.
- read_filters.
- read_data_validation.
- read_conditional_formatting.
- workbook_properties.
- connection/query metadata.
- calculation mode/status.
- search cells.

Write / edit actions:

- create workbook.
- add/delete/rename/copy/move worksheet.
- write values.
- write formulas.
- fill/copy ranges.
- clear contents / clear formats separately.
- insert/delete rows and columns.
- merge/unmerge cells.
- row height / column width / autofit.
- number formats.
- fonts/fills/borders/alignment.
- tables create/resize/style.
- sort.
- filter / clear filter.
- freeze panes.
- named ranges create/update/delete.
- data validation create/update/delete.
- conditional formatting create/update/delete.
- comments/notes.
- hyperlinks.
- insert/replace images.
- chart create/update/delete.
- pivot table create/refresh/update where provider supports it.
- formula recalculation.
- refresh external connections / Power Query only behind explicit network/mutation policy.
- protect/unprotect workbook or sheets with ephemeral passwords.
- save/save_as.
- export CSV/TSV.
- export PDF.
- print.

Advanced but guarded:

- VBA/macro discovery is read-only by default.
- Running a macro must be a DANGEROUS action, disabled by default, explicitly confirmed for each run, auditable, and never enabled merely because Full profile is selected.
- Do not lower Office Trust Center settings automatically.
- Never silently enable blocked macros.

Validation:

- render_excel_preview should expand beyond A1:C8 by accepting bounded sheet/range inputs.
- compare_workbook_layout should compare selected sheets/ranges, formulas, formats, dimensions, tables/charts metadata, and configurable invariants.
- formula-error checks for #REF!, #VALUE!, #DIV/0!, #NAME?, and other provider-reported errors.
- post-write verification of affected ranges.

### office_powerpoint

The PowerPoint tool should support real deck authoring, not only read/save-copy.

Read / inspect actions:

- inspect_presentation.
- list_slides.
- get_slide.
- get_slide_text.
- get_shapes.
- get_images metadata.
- get_tables.
- get_charts.
- get_notes.
- get_layout/master/theme metadata.
- presentation_properties.
- slide_size.
- hyperlinks/actions.
- transitions/animations metadata when provider supports it.

Write / edit actions:

- create presentation.
- add slide.
- duplicate slide.
- delete slide.
- reorder slide.
- change layout.
- edit title/body text.
- add/edit/remove textboxes.
- add/edit/remove shapes.
- add/edit/remove images.
- crop/resize/position images.
- add/edit tables.
- add/edit charts.
- add hyperlinks/action links.
- speaker notes.
- background.
- theme/layout/master changes where native provider safely supports them.
- slide size.
- transitions/animations where provider supports them.
- save/save_as.
- export PDF.
- export PNG/JPEG per slide.
- export video only as a long-running provider operation with progress/cancellation if the native app supports it.
- print.

Validation:

- render_slide_preview.
- render_presentation_preview.
- compare_presentation_layout.
- overflow/clipping detection where render metadata permits.
- verify slide count/order and required text after edits.

### office_outlook

Outlook must progress from header listing to a complete mail workflow while preserving privacy and send/delete confirmation boundaries.

Read actions:

- list_folders.
- list_messages with paging.
- get_message.
- get_headers.
- get_body with text/HTML modes.
- list_attachments.
- save_attachment to Active Project with file-safety checks.
- search messages by bounded query.
- conversation/thread lookup.
- unread/flag/category state.
- mailbox/profile status.

Write actions:

- create_draft.
- update_draft.
- add/remove recipients.
- attach/detach files.
- reply draft.
- reply-all draft.
- forward draft.
- set importance/category/flag.
- mark read/unread.
- move/copy message.

High-risk actions:

- send.
- delete / permanent delete.
- external recipient sends.
- sending attachments outside the organization when detectable.

These must use explicit confirmation and clear audit evidence. Draft creation should be safer than send and should not require the same risk level.

### office_calendar

Support Outlook/Microsoft 365 calendar as a dedicated semantic surface even if the underlying provider is Outlook COM or Graph.

Read actions:

- list calendars.
- get events.
- search events.
- get event details.
- availability/free-busy where provider supports it.

Write actions:

- create event.
- update event.
- move event.
- add/remove attendees.
- set reminders.
- recurrence.
- meeting location / online meeting metadata.

High-risk actions:

- send meeting invite/update/cancellation.
- delete/cancel event.
- respond accept/tentative/decline on behalf of the user.

### office_contacts

- list/search contacts.
- get contact.
- create/update contact.
- categories.
- company/title/phones/addresses.
- contact groups where provider supports them.
- delete is guarded/destructive.

### office_tasks

Where Outlook/Graph provider supports tasks:

- list task folders/lists.
- list/search tasks.
- create/update task.
- due date/reminder/importance/status/categories.
- complete/reopen.
- delete guarded.

Do not claim support if the configured provider exposes no compatible task API.

### office_onenote

Prefer Microsoft Graph / supported local provider.

- list notebooks.
- list sections.
- list pages.
- read page.
- search bounded page content.
- create page.
- append/update supported page content.
- add images/files where API supports it.
- move/copy page if provider supports it.
- delete only when supported and explicitly confirmed.

### office_onedrive

Cloud file-management surface through Microsoft 365 provider:

- list/search files/folders.
- metadata.
- download into Active Project.
- upload from Active Project.
- create folder.
- copy/move/rename.
- versions.
- sharing/link inspection.
- guarded sharing changes.
- guarded delete/restore.

Tokens must never be returned to ChatGPT.

### office_sharepoint

- list accessible sites with bounds.
- search sites.
- list drives/libraries.
- list/read list items.
- files/folders.
- upload/download.
- create/update supported list items.
- permissions/sharing changes only behind explicit confirmation.
- no tenant-wide crawling by default.

### office_teams

Use Microsoft Graph only for operations actually supported by configured permissions/APIs.

- list teams/channels accessible to the user with bounds.
- list recent channel messages where permitted.
- get message/thread.
- create draft payload / preview.
- send channel/chat message only with explicit confirmation.
- files should route through SharePoint/OneDrive provider.
- meeting/calendar semantics should reuse office_calendar instead of duplicating logic.

### office_access — Windows optional provider

If Microsoft Access is installed and provider health confirms automation support:

- inspect database objects.
- list tables/queries/forms/reports/modules.
- inspect table schema.
- SELECT/query read.
- create/update table/query objects.
- import/export Excel/CSV.
- run saved query with proper risk classification.
- form/report export when supported.
- database compaction/repair only as guarded maintenance.
- VBA execution disabled by default and separately dangerous.

This must never be advertised ready on hosts without Access.

### office_visio — Windows optional provider

If Visio is installed:

- list pages/layers/masters.
- read shapes/text/connectors.
- create/update/delete shapes.
- connect shapes.
- position/size/alignment.
- containers/layers.
- page properties.
- export PDF/SVG/PNG.
- validate expected shapes/connectors.

### office_project — Windows optional provider

If Microsoft Project is installed:

- project metadata.
- tasks.
- milestones.
- dependencies.
- resources.
- assignments.
- calendars.
- constraints.
- percent complete.
- cost/work fields where available.
- create/update tasks/resources/assignments.
- baseline operations with explicit confirmation.
- save/export PDF/Excel.

### office_publisher — legacy optional Windows provider

Only expose if an installed Publisher automation provider is detected and verified.

- inspect publication/pages/text/images.
- create/edit publication elements supported by automation.
- save/save_as/export PDF.
- report provider limitations truthfully.

Do not make Publisher a mandatory dependency for v5.5.0 release readiness.

### office_convert

Central conversion API with capability-driven routing:

- DOCX ↔ PDF where supported.
- XLSX → PDF/CSV.
- PPTX → PDF/images.
- supported Office legacy formats to modern formats when a real provider supports the conversion.
- no conversion result may be reported successful until output file existence, non-zero size, and target type are verified.

### office_batch

Provide bounded multi-operation orchestration for a single logical Office transaction.

Requirements:

- maximum operation count.
- ordered operations.
- same-document/workbook/deck transaction when provider supports it.
- dry-run plan.
- fail-fast by default.
- per-step result.
- cancellation.
- temp-copy / atomic replace for file mutations where possible.
- no implicit cross-app dangerous operation bundling. Sending mail or invitations inside a batch still requires explicit high-risk authorization.

## Shared Office document model

Introduce normalized types for common concepts so every provider does not invent incompatible shapes:

- OfficeFileRef.
- OfficeRangeRef.
- OfficeDocumentStructure.
- OfficeTextRun.
- OfficeStyleRef.
- OfficeTable.
- OfficeImageRef.
- OfficeComment.
- OfficeRevision.
- WorkbookRange.
- WorkbookCell.
- WorkbookTable.
- WorkbookChart.
- SlideRef.
- SlideShape.
- MailMessageRef.
- CalendarEventRef.
- ContactRef.
- CloudItemRef.
- ProviderCapability.
- OfficeOperationResult.
- OfficeMutationPlan.

Provider-specific raw objects must not leak directly into MCP responses.

## Provider implementation requirements

### Windows Office COM

Windows should be the first full-fidelity target because lnwjud already has a working Windows-native Office capability.

Implementation rules:

- Use isolated owned automation sessions where practical.
- Do not attach to or close a user’s unrelated interactive Office process unless explicitly requested and ownership is proven.
- Release COM references deterministically.
- Prevent orphan WINWORD.EXE / EXCEL.EXE / POWERPNT.EXE / OUTLOOK.EXE processes created by lnwjud.
- Cancellation must stop only lnwjud-owned work.
- Enforce operation timeouts.
- Detect Office not installed / app not installed / file locked / Protected View / modal dialog conditions and return actionable structured errors.
- Never disable Trust Center / Protected View globally.
- File mutations should work on a safe copy and replace only after verification when feasible.
- Preserve original file on failure.

### macOS

The current macOS Office backend is only a dependency boundary. v5.5.0 must implement and test real supported automation before marking actions ready.

Requirements:

- Detect installed Microsoft Office apps.
- Detect Automation permission availability.
- Use supported Apple Events/JXA/native automation paths where reliable.
- Maintain a per-action capability matrix.
- If an action is not supportable with validated fidelity, return unsupported rather than fake success.
- Open XML or Graph may be selected for operations not available through native automation, but the selected provider must be reported.

### Linux

Linux must not pretend that local Microsoft Office COM exists.

Supported strategy:

- LibreOffice UNO for validated local document/spreadsheet/presentation operations.
- Open XML for structural Office file operations.
- Microsoft Graph for Microsoft 365 cloud workloads.
- Per-action capability matrix and truthful limitations.

### Microsoft Graph / Microsoft 365

Add an optional OAuth provider with least-privilege scopes.

Requirements:

- OAuth tokens stored only in secure local storage.
- No access/refresh token in logs, tool outputs, diagnostics, crash reports, or release evidence.
- Scope acquisition should be incremental where possible.
- User can disconnect/revoke local credentials.
- Provider status shows scopes/capabilities without exposing secrets.
- Graph should be used only for workloads actually supported by the Graph API; do not promise full Word/PowerPoint object-model editing merely because the file is stored in OneDrive.
- Cloud file operations must respect Active Project boundaries when downloading/uploading local files.
- Network operations need explicit open-world metadata and audit events.

## Safety, permissions, and recovery

### Permission classes

READ:

- inspect/list/search/read content.
- metadata.
- preview/compare.
- capability/status.

WRITE:

- create/edit/save files.
- create drafts.
- update local/cloud items where reversible.

EXECUTE / elevated side effect:

- print.
- refresh external data.
- long-running exports.
- provider-native calculation/refresh operations that can contact external systems.

DANGEROUS / explicit approval:

- send mail.
- send meeting invites/updates/cancellations.
- delete cloud data or messages.
- permanent delete.
- run VBA/macros.
- overwrite source file without recovery capability.
- change sharing/permissions.
- publish externally.
- any operation that lowers Office security settings — v5.5.0 should normally deny rather than permit this.

### Dry-run

Every mutation-capable Office tool should support dry-run where a meaningful preview can be produced.

Dry-run should return:

- provider that would be used.
- files/items affected.
- operation count.
- destructive/high-risk steps.
- expected output paths.
- required confirmation level.
- dependencies/scopes required.

### File recovery

Reuse or generalize prepareExternalFileMutation.

For existing files:

- create replacement backup before overwrite.
- retain Recovery Trash/checkpoint metadata.
- expose recovery evidence in result.
- verify target exists and is readable after mutation before declaring success.

### Secret handling

Passwords for protected Office files, Graph tokens, mailbox credentials, and tenant secrets must be ephemeral or secure-storage backed. Never echo them into logs or tool results.

## Output and context economy

Every potentially large read requires:

- range/page/slide/message limits.
- continuation token or deterministic paging.
- response size caps.
- metadata indicating truncation.
- targeted read-after-inspect workflow.

Examples:

- Word: paragraph/section/page bounds.
- Excel: sheet/range bounds, row/column limits.
- PowerPoint: slide range.
- Outlook: message count + paging.
- SharePoint/OneDrive: bounded search result count.
- Teams: bounded channel/message windows.

## Office visual validation

Add first-class visual/structural validation tools rather than trusting write success alone.

Target surfaces:

- inspect_docx.
- render_docx_preview.
- compare_docx_layout.
- inspect_presentation.
- render_slide_preview.
- compare_presentation_layout.
- enhanced inspect_workbook.
- enhanced render_excel_preview.
- enhanced compare_workbook_layout.

Validation should distinguish:

- semantic equality.
- structural equality.
- visual/layout similarity.
- expected-field assertions.

For high-fidelity Windows operations, use native Office rendering/export when possible. Open XML inspection must not claim visual equivalence when it has not rendered the document.

## Tool catalog and discovery

All new Office tools must integrate with:

- upgrade catalog.
- tool contracts/schema registry.
- dynamic tool search/filtering.
- tool categories.
- readiness and dependency requirements.
- permission metadata.
- mutation inventory.
- audit target.
- supportsDryRun/supportsCancel.
- tool health.
- runtime fixtures.
- Tools page availability/remediation UI.

Recommended categories:

- office.
- word.
- excel.
- powerpoint.
- outlook.
- calendar.
- contacts.
- onenote.
- onedrive.
- sharepoint.
- teams.
- access.
- visio.
- project.
- document.

Descriptions must be action-oriented so ChatGPT can select the right app-specific tool rather than defaulting to computer_use.

## Office settings / UI

Add an Office & Microsoft 365 section to Settings or Tools detail surfaces.

Show:

- Local Office provider status.
- detected apps.
- provider for each app.
- Microsoft 365 connection state.
- scopes/capabilities summary.
- connect/disconnect action for Microsoft 365.
- optional LibreOffice/UNO status on Linux.
- permission warnings for Automation on macOS.
- macro execution policy, default OFF.
- external data refresh policy, default confirmation-required.
- diagnostics/remediation actions.

Do not expose raw OAuth tokens or app secrets.

## What's New UX

### User requirement

Add a small “?” icon immediately beside the version shown in the title bar.

Hover text in Thai:

ดูกันว่ามีอะไรอัพเดตใหม่

English equivalent:

See what’s new

Clicking the “?” opens a modal popup with details for the currently installed app version.

### Preserve update behavior

The current title-bar version button already performs update/check/install behavior through onUpdateAction. Do not replace that action.

Target title-bar composition:

- existing version/update button remains unchanged in purpose.
- new adjacent “?” button opens What’s New.
- sidebar version remains static unless a separate future requirement asks for another entry point.

### Modal requirements

Create a dedicated WhatsNewModal component.

Header:

- Thai: มีอะไรใหม่ใน v{version}
- English: What’s new in v{version}

Metadata:

- version.
- release date when present.
- optional short release summary.

Sections should be visually separated and appear only when they have content.

Recommended section keys:

- New / ใหม่.
- Office & Tools / Office และเครื่องมือ.
- Improvements / ปรับปรุง.
- UI & UX / หน้าตาและการใช้งาน.
- Fixes / แก้ไขบัค.
- Security & Reliability / ความปลอดภัยและความเสถียร.
- Platform / ระบบปฏิบัติการ.
- Developer / Runtime when end-user relevant.

Each item should support:

- title.
- short description.
- optional detail.
- optional badge such as NEW, IMPROVED, FIXED.
- optional related tool/app label.

Do not show empty headings.

### Release-note data model

Create a typed local release-note registry keyed by semantic version.

Suggested model:

ReleaseNote:
- version
- releaseDate
- summaryKey
- sections

ReleaseNoteSection:
- category
- titleKey
- items

ReleaseNoteItem:
- id
- titleKey
- descriptionKey
- badge
- tags

All visible copy should use the existing i18n translator and MessageKey system. Do not add props.locale === 'th' or isTh branches.

The registry should contain semantic data; Thai/English text belongs in i18n messages.

### Version lookup behavior

- Use appVersion already supplied to AppShell.
- Resolve release notes by exact installed version.
- If no exact entry exists, show a localized graceful-empty state rather than showing notes for the wrong version.
- Do not infer release notes from Git history at runtime.
- Do not require network access to open the modal.
- Future update metadata may optionally include remote release notes, but local bundled notes are authoritative for the installed version in v5.5.0.

### Accessibility

- The “?” control is a real button.
- aria-label localized.
- hover/focus tooltip.
- keyboard focusable.
- Enter/Space opens modal.
- Escape closes modal.
- focus moves into modal on open and returns to trigger on close.
- modal uses dialog semantics with aria-modal.
- scrolling content for small windows.
- close button has localized accessible name.
- title bar drag regions must not swallow clicks on the “?” button.

### Visual behavior

- Keep the button compact and aligned with the version badge.
- Reuse existing visual tokens/primitives instead of inventing a second design system.
- Modal should use current dark/gold application styling.
- Category cards/sections should have consistent spacing.
- Thai text must wrap correctly.
- Long release notes scroll inside the modal rather than stretching the application window.
- Responsive down to the minimum supported desktop window size.

## Proposed code organization

Exact names can change after implementation-time symbol review, but the plan should converge toward a shared architecture instead of adding logic to one large runtime file.

Capabilities:

- packages/capabilities/src/office/contracts.ts
- packages/capabilities/src/office/provider.ts
- packages/capabilities/src/office/provider-router.ts
- packages/capabilities/src/office/windows-com-provider.ts
- packages/capabilities/src/office/macos-provider.ts
- packages/capabilities/src/office/libreoffice-uno-provider.ts
- packages/capabilities/src/office/openxml-provider.ts
- packages/capabilities/src/office/graph-provider.ts
- packages/capabilities/src/office/worker.ts
- packages/capabilities/src/office/capability-matrix.ts

Existing packages/capabilities/src/office-backend.ts should either become the shared boundary/facade or be migrated carefully after references are inspected.

MCP/runtime:

- packages/mcp-server/src/office-runtime.ts
- packages/mcp-server/src/office-tool-contracts.ts
- packages/mcp-server/src/office-provider-status.ts
- update upgrade-catalog.ts.
- update upgrade-tool-contracts.ts.
- update tool-registry.ts.
- update mutation-policy.ts.
- update mutation inventory.
- update runtime fixtures/readiness.
- retain document-runtime.ts for document-specific shared inspection where appropriate, or delegate from it to OfficeSuiteRuntime.

Desktop:

- apps/desktop/src/renderer/features/release-notes/WhatsNewModal.tsx
- apps/desktop/src/renderer/features/release-notes/release-notes.ts
- apps/desktop/src/renderer/features/shell/AppShell.tsx
- apps/desktop/src/renderer/i18n/messages.ts
- apps/desktop/src/renderer/styles.css or the existing relevant style module.
- settings/tools surfaces for Office provider connection/readiness.

IPC:

Add IPC only when required. The What’s New modal should remain renderer-local if it only needs appVersion + bundled release notes. Do not add IPC merely to move static data around.

## Data and versioning contract for release notes

v5.5.0 should establish a reusable release-note process for later versions.

Rules:

- Every user-facing release can add one registry entry.
- Registry version must be valid semver and unique.
- Current app version may have zero notes during development, but release packaging should verify a finalized release-note entry exists before public release.
- set-version workflow should not automatically invent notes.
- release notes must be reviewed human-facing copy, not raw commit logs.
- README/GitHub release notes can be generated or cross-checked from the same semantic source later, but v5.5.0 does not need to make the desktop dependent on GitHub.

## Implementation phases

### Phase 0 — Re-baseline before coding

- Re-read AGENTS.md and active project rules.
- Confirm current dev HEAD and version after v5.4.x work has landed.
- Re-run Serena symbol/reference/implementation discovery on all current Office and AppShell surfaces.
- Confirm current tool count/catalog generation behavior.
- Confirm Windows Office provider contract.
- Confirm release pipeline version contract.
- Record any architectural drift from this plan before code changes.

### Phase 1 — Office contracts and provider router

- Define normalized Office domain types.
- Define app/action capability matrix.
- Define provider interface.
- Add deterministic provider routing.
- Preserve existing generic office compatibility.
- Add status/readiness reporting.
- Add bounded result helpers.

Exit criteria:

- Existing Office tools still work.
- New provider router can truthfully describe current capabilities.
- No app/action is reported ready without a provider.

### Phase 2 — Mutation/recovery foundation

- Generalize external-file safe mutation for Office.
- Dry-run plans.
- temp-copy / atomic replace where feasible.
- cancellation.
- owned Office process/session lifecycle.
- audit metadata.
- error normalization.

Exit criteria:

- simulated provider failure cannot replace original file incorrectly.
- owned process cleanup verified.
- permissions remain enforced.

### Phase 3 — Word full tool

Implement office_word and document inspect/render/compare.

Release-quality scenarios must include:

- create DOCX.
- edit text/styles.
- table operations.
- image insertion.
- headers/footers.
- comments.
- tracked changes.
- export PDF.
- post-edit inspection.
- recovery after failed mutation.

### Phase 4 — Excel full tool

Implement office_excel with formulas, formatting, tables, charts, pivots, validation, filters, freeze panes, import/export, and enhanced previews/comparison.

Release-quality scenarios must include:

- create workbook.
- write formulas and styles.
- create table and chart.
- sort/filter.
- detect formula errors.
- save/export.
- compare target ranges.
- recovery on failure.

### Phase 5 — PowerPoint full tool

Implement office_powerpoint + render/compare.

Scenarios:

- create deck.
- add/reorder slides.
- edit text.
- insert images/tables/charts.
- notes.
- export PDF/images.
- render verification.

### Phase 6 — Outlook, calendar, contacts, tasks

Implement complete mail draft/read/search workflow and calendar/contact/task tools.

Scenarios:

- search/read mail.
- save attachment safely.
- create draft.
- update draft.
- send path verified through approval boundary.
- calendar create/update/cancel path.
- contacts CRUD.
- tasks where provider supports them.

Do not send real external mail in generic CI.

### Phase 7 — Microsoft 365 cloud provider

Implement OAuth + secure storage + Graph provider for supported workloads.

Scenarios:

- connect/disconnect.
- status/scopes.
- OneDrive list/upload/download.
- SharePoint list/file operations.
- OneNote page read/create where supported.
- Teams bounded reads and confirmed send where supported.
- Outlook/Calendar cloud parity where API supports it.

### Phase 8 — Optional extended desktop apps

Implement Access, Visio, Project, and Publisher only behind dependency detection and capability matrices.

An app not installed must remain cleanly unavailable rather than causing health failures.

### Phase 9 — macOS/Linux provider completion

Implement and validate real provider actions.

- macOS native automation for supported installed apps.
- Open XML fallback.
- LibreOffice UNO on Linux.
- Graph cloud path on both.

Do not block v5.5.0 release on impossible parity, but do block claims of parity. Documentation and tool readiness must be exact.

### Phase 10 — Office visual verification

Add DOCX/PPTX render/inspect/compare and improve workbook previews.

### Phase 11 — What’s New modal

- Add typed release-note registry.
- Add v5.5.0 content.
- Add i18n keys for Thai/English.
- Add “?” beside version in AppShell.
- Preserve onUpdateAction on the existing version badge.
- Add accessible tooltip/modal.
- Add responsive styling.

### Phase 12 — Tool catalog, docs, release readiness

- regenerate tool catalog.
- update LNWJUD_CAPABILITIES.md.
- update README/FULL_README if those files remain authoritative at implementation time.
- document Office provider setup per platform.
- document Microsoft 365 OAuth scopes and privacy.
- update release notes.
- verify packaging includes required providers/assets.
- verify tool counts are generated rather than manually stale.

## Testing strategy

Follow lnwjud’s behavior-focused test rule: create/modify tests only for meaningful functional behavior, contracts, validation, integration, and regression risks. Do not add tests merely for wording, CSS, icon shape, labels, or formatting.

### Unit / contract tests

- schema action validation.
- provider selection.
- capability matrix.
- bounded outputs.
- permission classification.
- dry-run.
- recovery planning.
- secret redaction.
- release-note registry version lookup.
- What’s New modal open/close and locale behavior only where functional behavior is involved.

### Provider tests

Use provider fakes for deterministic CI, but do not treat fakes as proof that native Office works.

Windows real-provider acceptance on an Office-equipped test host should exercise temporary files for Word/Excel/PowerPoint and safe Outlook draft operations.

macOS/Linux acceptance should run only against the real provider being claimed ready.

### Integration tests

- tool registry → Office runtime → provider.
- mutation policy.
- Active Project path enforcement.
- Full Bypass semantics.
- cancellation.
- output recovery.
- Graph OAuth token redaction.
- tool availability/remediation.
- generated tool catalog.

### Desktop functional tests

- version update button still calls update action.
- “?” opens release notes without triggering update.
- modal resolves exact current version.
- locale switch updates modal text.
- keyboard open/close.
- missing release-note entry shows graceful state.

No dedicated test is required merely for the exact tooltip sentence or visual spacing.

## Real-provider acceptance evidence

Because “works in mocks” is not sufficient for this release, create an acceptance script or test harness that records non-secret evidence from real providers.

Minimum Windows evidence before release:

- Word installed and provider ready.
- create/edit/save/reopen DOCX.
- export DOCX → PDF.
- Excel create/edit/formula/chart/save/reopen.
- PowerPoint create/edit/save/export.
- Outlook folder read + draft creation/update without external send.
- no orphan Office process from the test run.
- generated files verified readable.
- source files recoverable after injected failure.

Microsoft 365 provider evidence when shipping Graph support:

- OAuth connection established on a test tenant/account.
- token redaction verified.
- OneDrive read/write test file.
- Outlook safe draft.
- calendar test event with cleanup.
- any Teams/SharePoint/OneNote actions advertised as ready must have their own real-provider evidence.

Never commit credentials or captured mailbox/document contents as evidence.

## Performance and reliability requirements

- Reuse Office application sessions only when safe; avoid per-cell COM startup.
- Batch range operations instead of one COM round trip per cell.
- Enforce operation-level timeout.
- Support AbortSignal/cancellation where provider can safely stop.
- Long export/refresh tasks expose progress when practical.
- Cache only safe metadata/capabilities, not document content or secrets by default.
- Close/dispose provider resources on success, failure, timeout, app shutdown, and cancellation.
- Telemetry should record tool/provider/action/latency/result code but not document bodies, cell contents, mail bodies, tokens, or sensitive filenames when redaction policy requires masking.

## Security requirements

- Maintain Active Project scoping for local file paths.
- Cloud item IDs are not local-path bypasses.
- Attachments downloaded from Outlook/Teams/SharePoint must pass file-safety boundaries.
- Protected View and Trust Center remain enabled.
- Macros default disabled.
- External data refresh is not automatic.
- Sending/publishing/sharing is explicitly confirmed.
- Provider errors must not dump COM objects, OAuth tokens, HTTP authorization headers, or raw sensitive payloads.
- Office-generated temporary files use owned temp locations and are cleaned safely.
- Do not invoke shell scripts for text/document mutation when native/structured provider actions exist.

## Backward compatibility and migration

- Existing office primitive remains callable.
- Existing inspect_workbook/render_excel_preview/compare_workbook_layout remain valid; they can delegate internally to OfficeSuiteRuntime.
- office_ppt and office_outlook remain valid aliases or compatibility tools until a documented deprecation cycle.
- docx_merge remains compatible and should route through office_word merge logic after parity is proven.
- Tool result shapes should preserve existing fields where clients may rely on them, while adding provider/readiness metadata additively.
- No saved user setting should be deleted as part of the Office migration.

## Documentation deliverables for v5.5.0

- Complete Office tool catalog.
- Per-platform provider matrix.
- Windows Office prerequisites.
- macOS Automation permissions.
- Linux LibreOffice/Graph limitations.
- Microsoft 365 OAuth setup.
- permission/safety explanation.
- macro/external-data policy.
- examples for Word, Excel, PowerPoint, Outlook, OneDrive/SharePoint.
- troubleshooting.
- privacy/redaction behavior.
- What’s New / release-note maintenance guide.

## v5.5.0 acceptance criteria

The release is complete only when all of the following are true:

1. Existing Office tools remain backward-compatible or have explicit compatibility aliases.
2. office_status truthfully reports app/provider/action readiness.
3. Word can create, read, materially edit, save, export, inspect, and recover a DOCX through at least one verified provider.
4. Excel can create, read/write formulas and formatting, perform table/chart operations, save/export, inspect, and verify affected ranges.
5. PowerPoint can create/edit real slides with text/media/table/chart operations and export/inspect the deck.
6. Outlook can read/search messages and create/edit drafts; send/delete remain confirmation-gated.
7. Calendar/contact/task capabilities are exposed only where the provider actually supports them.
8. Microsoft 365 OAuth is secure and tokens never appear in model-visible output/logs if Graph support ships in v5.5.0.
9. OneDrive/SharePoint/OneNote/Teams actions advertised as ready have real-provider evidence.
10. Access/Visio/Project/Publisher are dependency-gated optional tools and never falsely report ready.
11. Every Office mutation has a correct permission classification and recoverable-file strategy where applicable.
12. Macro execution is disabled by default and cannot be enabled silently.
13. Large Office reads are bounded/paged.
14. Real-provider acceptance has been run for every provider/app advertised as production-ready.
15. The title-bar version retains its existing update/check/install behavior.
16. A separate “?” button is visible beside the title-bar version.
17. Hover/focus on “?” shows localized “ดูกันว่ามีอะไรอัพเดตใหม่” / “See what’s new”.
18. Clicking “?” opens an accessible modal for the exact installed version.
19. Release-note modal supports Thai and English through the shared i18n system with no local isTh/locale branching.
20. Release notes are grouped into clean categories and empty categories are hidden.
21. Missing release-note data fails gracefully without showing the wrong version.
22. v5.5.0 ships with a finalized local release-note entry.
23. Tool catalog/docs/version contracts are regenerated and release checks pass.
24. No unrelated v5.4.x working-tree work is overwritten during implementation.
25. `search_text` treats query text as literal/fixed-string by default; characters such as `(){}?+*` do not become regex unless `regex: true` is explicit.
26. Valid explicit regex remains supported, while malformed regex/glob input returns a readable structured recoverable error with a retry suggestion.
27. `edit_file` remains exact-match only and never chooses a fuzzy candidate automatically.
28. Exact-edit conflicts are recoverable and distinguish multiple-match, stale-content, and text-not-found cases through structured conflict metadata.
29. Exact-edit mismatch responses detect and report CRLF/LF or whitespace-only drift when present.
30. A zero-match exact edit returns bounded nearby candidate context when available plus a re-read/retry suggestion, without mutating the file.
31. Regression coverage proves literal `failureLegacyBotName()` search, explicit regex behavior, malformed-regex recovery, stale exact-edit safety, line-ending/whitespace diagnostics, and recoverable conflict responses.

## Implementation guardrails for the future coding session

Before editing important Office logic:

- Activate the exact workspace in Serena and confirm Active Project.
- Inspect references/implementations for existing office, document runtime, capability provider, tool registry, mutation policy, AppShell, update action, and i18n symbols.
- Re-check this plan against the then-current dev HEAD.
- Keep existing safety/recovery abstractions instead of building parallel ones.
- Prefer shared provider/runtime components over app-specific duplicated plumbing.
- After logic changes, run targeted diagnostics and the smallest relevant functional/integration tests.
- Do not create tests for copy/CSS-only changes.
- Before release, run real-provider acceptance in addition to mocks/unit tests.

## Explicit non-goals

- Do not claim 100% feature parity across Windows, macOS, Linux, COM, Graph, UNO, and Open XML when the underlying APIs differ.
- Do not disable Office security mechanisms to make automation easier.
- Do not make computer_use the default implementation for Office document editing.
- Do not expose OAuth tokens or document/mail contents through telemetry.
- Do not auto-send mail, invites, Teams messages, or sharing links.
- Do not auto-run macros.
- Do not couple opening the What’s New modal to a network request.
- Do not replace the existing version update button with the release-notes button.
- Do not make `edit_file` silently fuzzy-match or auto-select among multiple candidate edit locations.
- Do not interpret `search_text` query strings as regex unless the caller explicitly requests regex mode.

## Definition of done

v5.5.0 is done when ChatGPT can use lnwjud as a truthful, permission-aware, recoverable Office automation layer for the Office/Microsoft 365 workloads that the installed providers actually support; the major Word/Excel/PowerPoint/Outlook workflows are verified against real providers; optional apps/cloud workloads expose exact readiness rather than fake parity; search/edit primitives recover safely from regex and stale-context errors without unsafe fuzzy edits; and end users can click the “?” beside the current version to read a clean Thai/English categorized summary of what changed in that installed release.
